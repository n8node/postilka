import { AdminGenerationNavPage } from "@/components/admin/AdminGenerationNavPage";

export const metadata = {
  title: "Меню генерации — Админ-панель",
};

export default function AdminGenerationNavRoute() {
  return <AdminGenerationNavPage />;
}
